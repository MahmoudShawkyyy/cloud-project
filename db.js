// Database configuration file

const config = {
    host: 'localhost',
    port: 5432,
    user: 'your_username',
    password: 'your_password',
    database: 'your_database'
};

module.exports = config;
